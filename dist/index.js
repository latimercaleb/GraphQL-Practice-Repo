'use strict';

var _graphql = require('graphql');

console.log('boo'); // Test with node index.js, this doesn't work because import is unrecognized without configuration of babel
// Root for loading graphql, schema and execution context